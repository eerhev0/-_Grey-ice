<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>灰冰名片</title>
<style>
*{margin:0;padding:0;box-sizing:border-box;font-family:sans-serif}
body{
  background:#fff;color:#222;
  padding:20px;
  display:flex;justify-content:center;align-items:center;
  min-height:100vh;
}
@media(prefers-color-scheme:dark){
  body{background:#121212;color:#eee}
}

/* 加载遮罩 */
#loader {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: #fff;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  z-index: 9999;
}
@media(prefers-color-scheme:dark){
  #loader {background: #121212;}
}

/* 进度条 */
.progress-bar {
  width: 80%;
  height: 6px;
  background: #e0e0e0;
  border-radius: 3px;
  overflow: hidden;
  margin-bottom: 20px;
}
@media(prefers-color-scheme:dark){
  .progress-bar {background: #333;}
}
.progress-fill {
  height: 100%;
  width: 0;
  background: #12b7f5;
  animation: loadProgress 1.5s ease-out forwards;
}
@keyframes loadProgress {
  0% {width: 0;}
  100% {width: 100%;}
}

/* 加载文字 */
.loading-text {
  font-size: 16px;
  color: #666;
  margin-bottom: 20px;
}
@media(prefers-color-scheme:dark){
  .loading-text {color: #aaa;}
}

/* 加载完成弹窗 */
#donePopup {
  background: #fff;
  padding: 20px 30px;
  border-radius: 12px;
  box-shadow: 0 5px 20 rgba(0,0,0,0.15);
  text-align: center;
  opacity: 0;
  transform: translateY(-20px);
  animation: showPopup 0.5s ease-out 1.5s forwards;
}
@media(prefers-color-scheme:dark){
  #donePopup {background: #1e1e1e; color: #fff;}
}
@keyframes showPopup {
  to {opacity: 1; transform: translateY(0);}
}
.done-title {
  font-size: 22px;
  font-weight: bold;
  margin-bottom: 10px;
}
.done-desc {
  font-size: 14px;
  color: #666;
  margin-bottom: 20px;
}
@media(prefers-color-scheme:dark){
  .done-desc {color: #aaa;}
}
#startBtn {
  padding: 10px 20px;
  background: #12b7f5;
  color: #fff;
  border: none;
  border-radius: 6px;
  font-size: 15px;
  cursor: pointer;
  transition: background 0.3s;
}
#startBtn:hover {
  background: #0ea6e0;
}

/* 名片卡片 */
.card {
  max-width: 600px;
  width: 100%;
  border: 1px solid #ccc;
  border-radius: 16px;
  padding: 30px;
  transition: all 0.3s ease;
  opacity: 0;
}
.card.show {
  animation: fadeIn 0.8s ease-out forwards;
}
@keyframes fadeIn {
  from {opacity: 0; transform: translateY(20px);}
  to {opacity: 1; transform: translateY(0);}
}
.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
}

/* 副简介暗色模式样式 */
.card.secret-mode {
  background: #3a0e0e;
  color: #ffb3b3;
}
.card.secret-mode .label {
  color: #ff9999;
}
.card.secret-mode .title {
  color: #ffcccc;
}

/* 点击名字彩蛋样式 */
.name {
  font-size: 36px;
  font-weight: bold;
  margin-bottom: 8px;
  cursor: pointer;
  transition: transform 0.2s;
}
.name:hover {
  transform: scale(1.05);
}
.title {
  color: #666;
  margin-bottom: 24px;
  font-size: 20px;
}
@media(prefers-color-scheme:dark){
  .title {color: #aaa;}
}
.item {
  margin: 16px 0;
}
.label {
  font-size: 12px;
  color: #888;
  text-transform: uppercase;
  margin-bottom: 4px;
}
@media(prefers-color-scheme:dark){
  .label {color: #aaa;}
}
.info {
  line-height: 1.6;
  font-size: 16px;
}

/* 弹跳动画 */
@keyframes bounce {
  0%,20%,50%,80%,100% {transform: translateY(0);}
  40% {transform: translateY(-8px);}
  60% {transform: translateY(-4px);}
}

/* 闪烁动画 */
@keyframes blink {
  0%,100% { opacity:1; }
  50% { opacity:0.7; }
}

/* 统一按钮样式 */
.btn {
  margin-top: 16px;
  padding: 12px 24px;
  color: #fff;
  border: none;
  border-radius: 8px;
  font-size: 15px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  width: 100%;
  text-align: center;
  display: block;
  text-decoration: none;
}

/* QQ按钮 蓝色 */
.qq {
  background: #0066ff;
  animation: bounce 2s infinite, glow-blue 2s infinite alternate;
}
@keyframes glow-blue {
  from {box-shadow: 0 0 5px #0066ff;}
  to {box-shadow: 0 0 15px #0066ff;}
}
.qq:hover {
  background: #0052cc;
}

/* 抖音按钮 黑红蓝 */
.douyin {
  background: #000;
  color: #fff;
  position: relative;
  overflow: hidden;
  animation: bounce 2s infinite 0.5s;
}
.douyin::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: linear-gradient(45deg, #fe2c55, #25f4ee);
  opacity: 0.9;
  z-index: -1;
}
.douyin:hover {
  opacity: 0.9;
}

/* 快手按钮 橙色 */
.kuaishou {
  background: #ff7800;
  animation: bounce 2s infinite 1s, glow-orange 2s infinite alternate 1s;
}
@keyframes glow-orange {
  from {box-shadow: 0 0 5px #ff7800;}
  to {box-shadow: 0 0 15px #ff7800;}
}
.kuaishou:hover {
  background: #e66a00;
}

/* 小红书按钮 红色 */
.xiaohongshu {
  background: #ff2442;
  animation: bounce 2s infinite 0.9s, glow-red 2s infinite alternate 0.9s;
}
@keyframes glow-red {
  from {box-shadow: 0 0 5px #ff2442;}
  to {box-shadow: 0 0 15px #ff2442;}
}
.xiaohongshu:hover {
  background: #e01f3a;
}

/* 微信按钮 绿色 */
.wechat {
  background: #07c160;
  animation: bounce 2s infinite 0.7s, glow-green 2s infinite alternate 0.7s;
}
@keyframes glow-green {
  from {box-shadow: 0 0 5px #07c160;}
  to {box-shadow: 0 0 15px #07c160;}
}
.wechat:hover {
  background: #06ad56;
}

/* 推特按钮 黑色+闪烁+弹跳 */
.twitter {
  background: #000;
  color: #fff;
  animation: bounce 2s infinite, blink 1.2s infinite;
}
.twitter:hover {
  background: #222;
}

/* 日志按钮 四色渐变 */
.log-btn {
  background: linear-gradient(90deg, #ffdd00, #ff3333, #33cc33, #3399ff);
  background-size: 400% 100%;
  animation: colorMove 6s linear infinite, bounce 2s infinite 1.2s;
}
@keyframes colorMove {
  0% { background-position: 0% 50%; }
  100% { background-position: 100% 50%; }
}
.log-btn:hover {
  opacity: 0.9;
}

/* 日志面板 */
#logPanel {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.7);
  display: none;
  justify-content: center;
  align-items: center;
  z-index: 99999;
  padding: 20px;
}
.log-box {
  background: #fff;
  max-width: 600px;
  width: 100%;
  padding: 30px;
  border-radius: 16px;
  box-shadow: 0 10px 30 rgba(0,0,0,0.3);
  max-height: 80vh;
  overflow-y: auto;
  position: relative;
}
@media(prefers-color-scheme:dark){
  .log-box {background: #1e1e1e; color: #fff;}
}
.log-box h2 {
  margin-bottom: 20px;
}
.log-box h3 {
  margin: 16px 0 8px;
}
.log-box p, .log-box li {
  line-height: 1.6;
  margin-bottom: 6px;
}
.log-box ul {
  padding-left: 20px;
}
.close-log {
  margin-top: 20px;
  padding: 10px 20px;
  background: #0066ff;
  color: #fff;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  width: 100%;
  font-weight: bold;
}

/* 日志右上角小彩蛋按钮 */
#easterBtn {
  position: absolute;
  top: 12px;
  right: 12px;
  width: 26px;
  height: 26px;
  border-radius: 50%;
  background: #666;
  color: #fff;
  border: none;
  font-size: 12px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0.6;
}
#easterBtn:hover {
  opacity: 1;
}

/* 爆炸表情包弹窗
